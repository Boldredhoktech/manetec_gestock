// app/(auth)/layout.tsx

export default function LayoutAuth({
                                       children,
                                   }: {
    children: React.ReactNode
}) {
    return <>{children}</>
}